---
import PageLayout from '~/layouts/PageLayout.astro';
import type { PageProps } from '~/types/astro';

export type Props = PageProps;

const { metadata, availableLanguages, currentLang } = Astro.props;

const layoutProps = {
metadata,
availableLanguages,
currentLang,
};
---

<PageLayout {...layoutProps}>
  <slot />
</PageLayout>
