---
// This is a redirect page that sends users to their preferred language
import { getBrowserLanguage } from '~/utils/language';

// Get the user's preferred language from the Accept-Language header
const preferredLanguage = getBrowserLanguage(Astro.request.headers.get('accept-language'));

// Redirect to the appropriate language page
return Astro.redirect(`/${preferredLanguage}/guests`);
---
