---
import PageLayout from '~/layouts/PageLayout.astro';
import GuestOverviewLayout from '~/components/guest/GuestOverviewLayout.astro';
import { getTranslations } from '~/utils/translations';

const t = getTranslations('de');
const metadata = {
  title: `${t.allGuests} | ${t.title}`,
  description: `Treffen Sie alle Gäste, die im ${t.title} Podcast aufgetreten sind.`,
  type: 'website' as const,
  canonicalUrl: Astro.url.href,
  alternateLanguages: {
    en: '/en/guests',
    de: '/de/guests',
    es: '/es/guests',
    nl: '/nl/guests',
  },
};

const availableLanguages = ['en', 'de', 'es', 'nl'] as const;
---

<PageLayout {...{ metadata, availableLanguages, currentLang: 'de' }}>
  <GuestOverviewLayout language="de" />
</PageLayout>