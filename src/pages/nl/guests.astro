---
import PageLayout from '~/layouts/PageLayout.astro';
import GuestOverviewLayout from '~/components/guest/GuestOverviewLayout.astro';
import { getTranslations } from '~/utils/translations';

const t = getTranslations('nl');
const metadata = {
  title: `${t.allGuests} | ${t.title}`,
  description: `Ontmoet alle gasten die te gast zijn geweest in de ${t.title} podcast.`,
  type: 'website' as const,
  canonicalUrl: Astro.url.href,
  alternateLanguages: {
    en: '/en/guests',
    de: '/de/guests',
    es: '/es/guests',
    nl: '/nl/guests',
  },
};

const availableLanguages = ['en', 'de', 'es', 'nl'] as const;
---

<PageLayout {...{ metadata, availableLanguages, currentLang: 'nl' }}>
  <GuestOverviewLayout language="nl" />
</PageLayout>