---
import { LanguageSchema } from '~/utils/component-validation';
import type { z } from 'zod';

interface Props {
  language: z.infer<typeof LanguageSchema>;
}

const { language } = Astro.props;
// TODO: Implement guest grid component with filtering by language and sorting capabilities
// The language prop will be used to filter guests based on their content language
---

<div class="mx-auto max-w-7xl px-4 sm:px-6 py-8">
  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
    <div class="text-center">
      <p class="text-muted">Guest list coming soon...</p>
    </div>
  </div>
</div>
