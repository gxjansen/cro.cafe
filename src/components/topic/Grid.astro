---
import { LanguageSchema } from '~/utils/component-validation';
import type { z } from 'zod';

interface Props {
  language: z.infer<typeof LanguageSchema>;
}

// @ts-ignore - Language prop will be used when implementing language-based filtering
const { language } = Astro.props;
// TODO: Implement topic grid component with filtering by language and sorting capabilities
// The language prop will be used to filter topics based on their content language
---

<div class="mx-auto max-w-7xl px-4 sm:px-6 py-8">
  <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
    <div class="text-center">
      <p class="text-muted">Topic list coming soon...</p>
    </div>
  </div>
</div>
